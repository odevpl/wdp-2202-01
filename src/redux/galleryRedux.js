/* selectors */
export const getAll = ({ gallery }) => gallery;

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
